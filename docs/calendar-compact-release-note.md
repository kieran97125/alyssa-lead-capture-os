# Compact marketing calendar tasks

Temporary release note for PR review. The calendar month view keeps at most three compact task rows visible per day, reveals task details and destructive actions on hover/focus, and places additional tasks behind a floating `+N more` list so day cells do not grow with task volume.
