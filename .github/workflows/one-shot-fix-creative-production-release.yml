name: One-shot Fix Creative Production Release

on:
  push:
    branches:
      - fix/creative-job-list-density-requester
    paths:
      - .github/workflows/one-shot-fix-creative-production-release.yml

permissions:
  contents: write

concurrency:
  group: one-shot-fix-creative-production-release
  cancel-in-progress: false

jobs:
  correct-and-verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      CI: true
      ALYSSA_E2E_FIXTURES: 1
      E2E_ADMIN_PASSWORD: playwright-ci-password
      E2E_MASTER_PASSWORD: playwright-ci-master-password
      LAUNCHHUB_ADMIN_PASSWORD: playwright-ci-password
      LAUNCHHUB_MASTER_PASSWORD: playwright-ci-master-password
      LAUNCHHUB_ADMIN_SESSION_SECRET: playwright-ci-session-secret-at-least-32-characters
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: fix/creative-job-list-density-requester
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply Creative Production release corrections
        run: python3 scripts/fix-creative-production-release.py

      - name: Install dependencies
        run: npm ci

      - name: Verify production build and contracts
        run: npm run build

      - name: Build Storybook
        run: npm run build:storybook

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Refresh deterministic Creative visual baselines
        run: npx playwright test e2e/creative-production.spec.ts --update-snapshots

      - name: Verify Creative Production interactions
        run: npm run test:creative

      - name: Verify design and accessibility
        run: npm run test:design

      - name: Verify full product regression
        run: npm run test:e2e

      - name: Commit verified correction and remove one-shot files
        shell: bash
        run: |
          rm .github/workflows/one-shot-fix-creative-production-release.yml
          rm scripts/fix-creative-production-release.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add --all
          git diff --cached --check
          git status --short
          git commit -m "Fix Creative Production release and visual gates"
          git push origin HEAD:fix/creative-job-list-density-requester
